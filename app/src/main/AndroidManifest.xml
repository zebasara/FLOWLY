<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos de red -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- GPS / Actividad física -->
    <!-- ACCESS_BACKGROUND_LOCATION NO declarado: con foreground service tipo
         "location" no se necesita, y declararlo requiere revisión especial de Play Store -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- Notificaciones -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Vibración para feedback táctil -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Unity Ads: optimización de tipo de red (elimina warning "missing permission").
         No requiere aprobación del usuario — se concede automáticamente en instalación. -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE"
        android:maxSdkVersion="29" />

    <application
        android:name=".FlowlyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.FLOWLY"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="31">

        <!-- AdMob App ID -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-5863137241796972~6439998100" />

        <!-- AppLovin MAX SDK Key (inyectado desde secrets.properties → manifestPlaceholders) -->
        <meta-data
            android:name="applovin.sdk.key"
            android:value="${applovinSdkKey}" />

        <!-- Resuelve conflicto entre play-services-ads y firebase-analytics
             sobre cuál config de Ad Services usar. AdMob gana. -->
        <property
            android:name="android.adservices.AD_SERVICES_CONFIG"
            android:resource="@xml/gma_ad_services_config"
            tools:replace="android:resource" />

        <!-- SplashActivity: punto de entrada del launcher -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.FLOWLY.Splash"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:configChanges="keyboard|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize" />

        <!-- Servicio de rastreo GPS en primer plano -->
        <service
            android:name=".services.TrackingForegroundService"
            android:foregroundServiceType="location"
            android:exported="false" />

    </application>

</manifest>
